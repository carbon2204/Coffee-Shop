    # Если SubCA для целостности заказов не создан, проверка не выполняется
        if "Целостность заказа" not in self.subcas:
            messagebox.showerror("Ошибка", "SubCA для проверки целостности заказов не создан.")
            return

        subca_obj = self.subcas["Целостность заказа"][3]
        # Здесь можно реализовать реальную проверку целостности заказов,
        # например, вычислить хэш данных заказов из БД и сравнить с эталоном,
        # либо подписывать отчет с помощью SubCA и проверять подпись.
        # Для демонстрации имитируем отчет:
        results_text = "Заказ 1: ОК\nЗаказ 2: ОК\nЗаказ 3: ИЗМЕНЁН\n"
        self.show_results("Целостность заказов", results_text)